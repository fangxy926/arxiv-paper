# LLM API Configuration (OpenAI Compatible)
OPENAI_API_BASE=https://your-api-endpoint.com/v1
OPENAI_API_KEY=your-api-key-here
OPENAI_MODEL=gpt-4o-mini

# arXiv 搜索配置
ARXIV_DAYS_BACK=7           # 往前检索多少天的论文（默认7天）
ARXIV_MAX_RESULTS=50        # 每个搜索词返回的最大结果数（默认50）


# 主题配置
TOPICS=医疗大模型,医疗数据集,医疗智能体

# 关键词配置（逗号分隔，空值跳过初筛）
FILTER_KEYWORDS=medical,clinical,healthcare,hospital,patient,diagnosis,disease,treatment,therapy,pharmaceutical,drug,radiology,pathology,biomedical,bioscience,ehr,electronic health,medical record,symptom,prognosis,triage,oncology,cardiology,neurology,dermatology,ophthalmology,genomic,genetic,language model,llm,large language model,transformer,encoder,decoder,decoder-only,encoder-decoder,pre-trained,instruction tuning,rlhf,fine-tuned,domain adaptation,foundation model,generative ai,dataset,benchmark,corpus,training data,evaluation set,agent,multi-agent,agentic,reasoning,chain-of-thought,cot,tool use,function call,autonomous,workflow,decision support,multimodal,vision-language,image-text,medical imaging
